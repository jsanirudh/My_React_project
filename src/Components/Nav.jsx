import React from "react";
import styles from "./container.module.css";

const Nav = () => {
  return (
    <nav className={styles.nav}>
      <h1>Food App</h1>
    </nav>
  );
};

export default Nav;
